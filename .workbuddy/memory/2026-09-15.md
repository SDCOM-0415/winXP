# 2026-09-15 工作日志

## 全量对齐与补应用（提交 16a0406 → ecde30e）

### 已完成的对齐面（对照 Reborn XP 的 CSS 规格）
| 面 | 状态 |
|---|---|
| 任务栏背景 / 托盘 / 任务栏按钮（含几何） | ✅ 09-14 已对齐 |
| 窗口框架 / 标题栏 / 窗口按钮 | ✅ 16a0406 已对齐 |
| 开始菜单 | ✅ **核对后发现原本就已与基准完全一致，无需改动** |
| 右键菜单 | ✅ ecde30e 已对齐 |
| 我的电脑 / 资源管理器、控件（controls.css）、applayouts.css | ⬜ 未开始 |

### 右键菜单对齐的具体差异（ecde30e）
| 项 | 原值 | 基准值 |
|---|---|---|
| 菜单行高 | 20px | **17px** |
| 左边距 | 24px | 21px（= li padding 1px + 图标列 16px + 文本 padding 4px） |
| 外框 | `#7f9db9` | **`#aca899`**（`--3dLightShade`） |
| 禁用项字色 | `#808080` | `#8c8a7e` |
| 分隔线 | `margin 3px 2px`，底色 `#d6d2c2` | `margin 3px 1px 5px 1px`，底色 `#aca899` |
| 悬停底色 | `#2f71cd` | **`#316ac5`**（`--highlightColor`） |
| 子菜单箭头 | `'▶'` 8px / line-height 20px | `'▼'` 9px / 19px + `rotate(-90deg) scaleY(.6)` |

关键取色变量（基准 `css/luna/blue.css` 内定义）：
`--3dLightShade: #aca899`、`--menuText: #000`、
`--highlightColor: #316ac5`、`--highlightText: #fff`

### 本轮新增应用
| 应用 | 说明 |
|---|---|
| 命令提示符（631dca5） | 接可写 VFS，支持 DIR/CD/TYPE/MD/RD/DEL/TREE 等 |
| 音量控制（ecde30e） | 五路混音推子，主音量与任务栏托盘共用 `prefs.volume`；托盘图标双击打开 |

**新增应用时要记得检查开始菜单里是否已有入口**——命令提示符和音量控制的入口
**本来就在 `FooterMenuData.js` 里**（图标都 import 好了），只是 `onClickMenuItem`
没有分支、点了会落进 ErrorBox。也就是说"补应用"很多时候只是补一个分支。

### 应用总数
原 8 个 → 现 12 个（+ 命令提示符、音量控制、计算器、写字板）。基准 19 个。

## 本轮追加（提交 314021d → 7a02596）

### 滚动条对齐（39ecdad）—— 一个之前完全漏掉的面
**项目此前完全没有滚动条样式**，所有可滚动窗口（我的电脑、记事本、命令提示符、
显示属性）用的都是浏览器默认灰滚动条，而基准是 Luna 蓝色滚动条。
规格在 `css/luna/controls.css` 的 `appcontents ::-webkit-scrollbar` 系列：
- 尺寸 17×17，隐藏箭头按钮
- 轨道 `linear-gradient(to right,#f3f1ec,#fefefb)` + `inset 1px 0 #eeede5` 等
- 滑块底色 `#c9d8fc→#b9cdfb`，多层 inset 阴影，`border 1px solid #fff`，圆角 3px
- 抓手位图：纵向常态用 **scrollgrip_vertical_active.png**（注意基准纵向常态就用 active）、
  悬停用 scrollgrip_vertical_hover.png；横向用 scrollgrip_horizontal.png
- **横向的 hover/active 两个抓手位图爬取副本里没有**，横向这两态以配色变化代替
- 经典主题覆盖为 `#d4d0c8` + 3D 立体滑块

**注意**：CSS 里写 `url(../assets/...)` 是可以的（CRA 的 css-loader 会处理相对路径），
不必像窗口按钮位图那样从 JS 注入。因为这两处的需求不同：
CSS 里能写死的就直接写，需要随主题在 JS 侧切换的才走 CSS 变量注入。

### 写字板（7a02596）
- 放弃了原计划的「磁盘清理」：VFS 整盘文本总量只有 **6762 字符**、目录体积无从取得，
  而磁盘清理的核心就是显示可回收体积 → 用真实数据会难看，编造数字没意义。
  **判断依据：宁可不做，不要伪造数据。**
- 写字板用 `document.execCommand` 实现富文本（加粗/斜体/下划线/对齐/项目符号/
  字体字号），文件读写接 VFS，内容以 HTML 存 `contents`
- 工具栏用文字标签而非图标：手上没有写字板工具栏对应位图，
  套用 `ui/nav/` 的导航图标会比文字更失真

### 校验脚本的两个坑（都已修）
1. **展开语法误报**：`...target` 前面是点号，会被
   `(?<![\w.$])name(?![\w$])` 的边界正则漏算，导致"未使用声明"误报。
   修法：计数前先把源码里的 `...` 替换成空格再匹配。
2. **eslint-disable 的反噬**：用 `[]` 空依赖 + `eslint-disable-next-line
   react-hooks/exhaustive-deps` 来只跑一次 effect 是危险写法——
   若规则未触发，会报"未使用的 disable directive"，
   而 CI 下告警即错误。正确做法是**用 ref 守卫** + 写完整依赖列表。

## 下一步待办
1. 继续补应用：系统信息、图片查看器、录音机、Windows Media Player、
   磁盘碎片整理程序
2. 对齐「我的电脑 / 资源管理器」内容区（`appcontents` / `appcontentholder` /
   `appnavigation`）
3. `applayouts.css`（55KB）里的各应用内部布局
4. 3D 弹球（见 MEMORY.md，用户指定的未来工作）

## 补应用的「数据可得性」筛选原则（重要，避免走弯路）

补应用前先问一句：**这个应用要显示的数据，我手上有没有真实的？**

| 应用 | 数据来源 | 结论 |
|---|---|---|
| 命令提示符 | VFS 真实文件树 | ✅ 已做 |
| 音量控制 | prefs 真实状态 | ✅ 已做 |
| 计算器 | 纯计算 | ✅ 已做 |
| 写字板 | VFS 文本内容 | ✅ 已做 |
| 字符映射表 | Unicode 码位（标准数据） | ✅ 已做 |
| **磁盘清理** | 需目录体积 | ❌ **放弃**：VFS 整盘文本仅 6762 字符，无体积字段 |
| **系统信息** | 需 CPU/BIOS/内存等硬件信息 | ⚠️ 只能编造硬件参数，需谨慎（可只展示真实项：屏幕分辨率、UA、时区、主题、VFS 统计） |
| 图片查看器 | 需图片二进制 | ⚠️ VFS 只存文本，需先设计图片存储方式 |

## 字符映射表（3f6e99b）
- 13 个字符集分区，全部由 Unicode 码位区间直接生成，未预置数据表
- 与 XP 一致**不带菜单栏**（原版只有工具栏 + 状态栏），所以没建 dropDownData
- 应用注册惯例再确认一次：**不带菜单栏的应用就不要建 `dropDownData.js`**

---

## ⚠️ 提交策略变更（2026-09-15，用户明确要求）

**只在用户明确说"提交"时才 commit/push。**

原因：项目部署在 EdgeOne，**每次 push 都会触发一次构建，EdgeOne 有构建次数限制**。
自动推送会白烧额度。

做法：改完代码留在工作区即可，一轮任务做完就停下汇报，不要以"提交推送"收尾。
这条**覆盖**了本轮之前"每轮改完直接提交推送"的旧习惯。
（已同时写入 `~/.workbuddy/MEMORY.md` 与项目 `MEMORY.md`）

---

## 图片查看器 + 图片资源机制（**未提交，待用户指示**）

### 关键发现：图片约定项目里本来就有
- VFS 的**图片节点把资源路径存在 `contents` 字符串上**，例如
  `"contents": "res/pictures/bluehills.jpg"`
- `C:\Documents and Settings\All Users\Shared Pictures\Sample Pictures` 下
  本来就有 XP 的四张示例图（Blue Hills / Sunset / Water Lilies / Winter）**条目**
- 但这些 `res/pictures/*.jpg` 是**悬空引用**：项目和爬取副本里都没有这些文件
- E 盘（Goodies CD）那 4 张 Microsoft 怀旧图在参考站里是 `microsoft_nostalgic.zip` 的内容，
  爬取时被解到了 `vfs/E/microsoft_nostalgic/`

### 做了什么
1. 新增 `src/assets/pictures/microsoft_nostalgic/`（4 张 jpg，共 840K）
2. `vfs/index.js` 新增图片资源解析，沿用项目已有的 `require.context` 约定：
   - `pictureMap`：以 `assets/pictures` 下的相对路径为键（递归）
   - `pictureByName`：按文件名兜底索引
   - `resolvePicture(contents)`：支持 `data:` / `http(s):` / 项目内相对路径三种写法，
     **解析不到返回 null，由调用方显示占位提示，不伪造图片**
   - `isImageName(name)`：按扩展名判断
   - `listPictures()`：列出全部图片（备用）
3. `drive_e_goodies.json` 新增 `microsoft_nostalgic` 目录，4 个文件节点的
   `contents` 写**精确相对路径**（如 `microsoft_nostalgic/xxx.jpg`），
   与 `require.context` 的键一一对应，不依赖兜底匹配
4. 新增 `apps/ImageViewer/`：上一张 / 下一张（同目录图片序列）、
   放大 / 缩小 / 实际大小 / 最佳适应 / 顺逆时针旋转、状态栏
5. 文件关联：`MyComputer.openEntry` 与 `WinXP.onDoubleClickIcon` 都加了图片分支

### 两个踩到的坑
1. **图片必须先于 `isEditableText` 判断**：图片节点的 `contents` 是字符串，
   `isEditableText` 会返回 true，不先分流的话图片会被记事本当文本打开。
2. **「实际大小」不能用 CSS 的 max-width 拟合**：初版把 `max-width/max-height:100%`
   与 `transform: scale()` 叠加，导致 100% 并不是真实像素。
   改成：最佳适应交给 CSS 约束（scale 固定 1），其余模式去掉约束、
   按 `zoom/100` 缩放，这样 100% 才是真正的实际大小。
   （顺带丢掉了原本需要的舞台尺寸测量与图片自然尺寸记录）
3. 又差点写 `eslint-disable react-hooks/exhaustive-deps`，已改用 ref 守卫。
4. **prettier 会按默认 tabWidth=2 重排 JSON**，把 `drive_e_goodies.json`
   从 4 空格改成 2 空格，产生 172 行无谓 diff、且与兄弟文件 `drive_c_system.json`
   （4 空格）不一致。**JSON 不要交给 prettier 处理**，用 `JSON.stringify(d,null,4)` 写回即可，
   diff 降回 27 行。注意 CI 的 prettier 规则只作用于 JS，不管 JSON，所以无风险。

### 待用户确认
- 这 4 张图是微软的怀旧宣传素材，与项目已打包的其它 XP 资源同源。
  用户 2026-09-15 明确回复**"不用移除"** → 保留
- `Sample Pictures` 那 4 条悬空引用未处理，打开会走占位提示

---

## 三维弹球调研（2026-09-15，**未写代码，等用户拍板**）

### 查清了参考站那份弹球的真实来源
- `res/sites/pinball/3DPinballSpaceCadet.js` 是 **9.28MB base64 的单行文本**
- 解码后是 **4.33MB 的有效 WASM**（magic `\0asm\1\0\0\0` 正确）
- WASM 内可读字符串给出确凿证据：
  - `Project home: https://github.com/k4zmu2a/SpaceCadetPinball`
  - `Original game by Cinematronics, Microsoft`
  - `/home/alula/opt/emsdk/...`（`alula` 是网页版作者）
  - `GPL`
- `PINBALL.DAT` 在 WASM 内只出现 1 次 → 符合 Emscripten `--preload-file` 的特征，
  说明**游戏数据是内嵌的、这份 WASM 自包含**，A 方案技术上可行

**结论：它不是参考站的原创代码**，而是社区开源项目 SpaceCadetPinball
（微软原版 3D 弹球的反编译重写）的 Emscripten 网页移植版。
参考站自己的部分只有一个 9.7KB 接入壳 + 一个含 `<canvas id="canvas">` 与
Emscripten `Module` 对象的独立页面（还把 `c` 键映射成 `/`、`r` 键映射成 `F2`）。

### 本轮已完成（无需代码授权、任何方案都要用）
- 移植媒体素材到 `src/assets/pinball/`：
  `splash.png`（33KB 启动图）、`bg.mp3`（2.8MB 背景音乐）。共 2.8MB

### 本轮已做的小优化（**会改变外观，已告知用户可回退**）
- 开始菜单「三维弹球」的图标由 `windowsIcons/svg/Pinball.svg`（**766KB**）
  换成 `fileIcons/pinball.png`（**3KB**），同时消掉一条 Babel 大文件降级提示

### 顺带查出的系统性问题（**未动手，等用户决定**）
`FooterMenuData.js` 共 56 个图标导入，其中 **10 个超过 500KB，合计约 6.4MB**：

| 体积 | 变量名 | 当前文件 | fileIcons 里有对应小图吗 |
|---|---|---|---|
| 744 KB | `magnifier` | Magnifier.svg | 无 |
| 695 KB | `update` | Windows Update.svg | ✅ `winupdate.png` |
| 674 KB | `compatibility` | Wizard.svg | 无 |
| 661 KB | `transfer` | File and Settings Transfer Wizard.svg | 无 |
| 631 KB | `cleanDisk` | Disk Cleanup.svg | ✅ `diskclean.png` |
| 627 KB | `messenger` | Windows Messenger.svg | ✅ `messenger.png` |
| 620 KB | `connection` | Network Connections.svg | 可用 `assets/ui/tray/connections.png` |
| 554 KB | `mediaPlayer` | Windows Media Player 10.svg | ✅ `wmplayer.png` |
| 551 KB | `tour` | Tour XP.svg | ✅ `xptour.png` |
| 540 KB | `connectionWizard` | New Internet Connection.svg | 无 |

**不擅自批量替换的原因**：换图标会改变开始菜单外观，而用户此前明确
对"我擅自改动观感"表达过不满。数字摆出来让用户决定。
（`assets/windowsIcons/svg/` 整个目录 187MB、135 个文件超 500KB，是历史遗留问题）

---

## 三维弹球：采用 A 方案接入（**未提交，等用户指示**）

用户 2026-09-15 选定 **A 方案（原样引入参考站那份 WASM）**，并同意保留图片素材。

### 接线方式（关键决定：放 public/，不进 bundle）
- 运行时文件全部放 **`public/pinball/`**（合计 12M），**不参与打包**：
  | 文件 | 体积 | 来源 |
  |---|---|---|
  | `3DPinballSpaceCadet.js` | 9.4M | 上游编译产物（GPL，见 NOTICE.md） |
  | `bg.mp3` | 2.8M | 参考站媒体素材 |
  | `splash.png` | 33K | 参考站媒体素材 |
  | `index.html` | — | **本项目自己写的宿主页面** |
  | `NOTICE.md` | — | **本项目写的第三方组件声明** |
- CRA 会把 `public/` 原样拷进构建产物，所以这 9.4M **只在用户真正打开弹球时才请求**，
  不进主 JS bundle、不影响首屏。这是相比"import 到 src"最大的收益。
- 已删除先前放在 `src/assets/pinball/` 的那两个媒体文件，避免 2.8M 双份

### 那份 JS 的内部构成（已确认）
1. `var WASM_FILE = "data:application/wasm;base64,..."` → 6.05M base64，解码后 **4.33MB WASM**
2. `var DATA_FILE = "data:application/octet-stream;base64,UklGR..."` → `RIFF/WAVE` 头，
   即**游戏音频也内嵌**
3. Emscripten 加载器，末尾调 `run()` **自动执行**
→ 完全自包含，`PINBALL.DAT` 由 `--preload-file` 打入，运行时不需再下载

### 宿主页面的实现要点
- 用 **iframe** 承载，而不是直接挂 canvas。原因：它是一份自带文档与输入处理的
  完整 Emscripten 应用，隔离在独立文档里才不会和主应用的全局键盘处理
  （屏保空闲计时等）互相干扰
- `window.Module` 必须在 `3DPinballSpaceCadet.js` **之前**定义（Emscripten 加载时读取）
- **键盘转发**：在 document 的**捕获阶段**拦下游戏按键，
  `preventDefault + stopPropagation` 后**定向重新派发给 canvas**，
  这样无论焦点在哪都只送达一次，也不会误吞浏览器快捷键（跳过了带修饰键的）
- 转发键位：`z` `Z` `/` `?` 空格 回车 方向键 `F2` `F3` `F4` `Esc`
- `bg.mp3` 是**原版 MIDI 背景音乐的替代品**（WASM 播不了 MIDI）。
  浏览器禁止无交互自动播放，所以改成**首次 mousedown/keydown 时启动**，
  并在右上角放了音乐开关
- splash 在 `Module.onRuntimeInitialized` 时淡出，另有 15s 兜底定时器

### 没有加菜单栏（重要）
3D 弹球是 Win32 程序，**它的菜单栏（游戏/选项/帮助）由它自己在 canvas 上绘制**。
所以本应用只提供承载 iframe 的窗口，**不叠加自己的 dropDownData**，
否则会出现两条菜单栏。也因此没有建 `dropDownData.js`。

### 归属与许可（按 A 方案时的承诺完成）
- `public/pinball/NOTICE.md`：写明上游项目、仓库地址、GPL 许可、原版版权方，
  并说明"作为独立未经修改的第三方组件分发、本项目 MIT 不覆盖该文件"
- `README.md` 的「许可声明」下新增子节说明该 GPL 组件，
  「致谢」新增 SpaceCadetPinball 与 alula 两条

### ⚠️ 提交前需要注意
`public/pinball/3DPinballSpaceCadet.js` 是 9.4MB，**一旦提交就永久留在 git 历史里**
（本项目不做历史改写）。用户此前对 EdgeOne 构建次数敏感，提交前值得再确认一次。
另外 `build/index.html` 的缓存时间戳后处理**只作用于根 index.html**，
`public/pinball/index.html` 不会自动加时间戳，若日后更新该文件需考虑 CDN 缓存。

---

## 窗口客户区对齐（**未提交**）

### 发现：`.app__content` 完全没有边框与内阴影
基准的 `appcontents` 规格（`css/luna/modules.css`）给每个窗口的客户区都加了：

```css
margin: 0 2px 2px;
background-color: var(--contentColor);   /* #fff */
border: 1px solid;
border-color: #777792 #66667e #66667e #66667e;
box-shadow: -1px 0 0 #a8a9bb, 1px 0 0 #a8a9bb, 0 1px 0 #a8a9bb;
letter-spacing: -.2px;
box-sizing: border-box;
```

而我们的 `.app__content` 只有 `flex/position/margin-top/height`，**一条视觉规则都没有**。

### 基准对「对话框」用的是另一套值
```css
app.dialogbox appcontents {
    box-shadow: -1px 0 0 #fbfcfd, 1px 0 0 #fbfcfd, 0 1px 0 #fbfcfd;
    margin: 0 1px 1px;
    background-color: var(--mainColor);   /* #ece9d8 */
}
```
本项目里 **`header.invisible` 正好对应对话框**（ErrorBox / AboutWindows / Winamp），
所以用 styled-components 的 `header` prop 分两套取值。

### 本次只加了视觉部分，**故意没加那 2px 外边距**
窗口本身已有 `padding: 3px`，客户区距边缘本来就是 3px；
再加 2px 只是变成 5px，视觉差异很小，而加外边距会改变**每一个窗口**的尺寸计算、
有布局回归风险。所以只加 `border + box-shadow + background + letter-spacing + box-sizing`，
**不动定位**。

### 尚未对齐的（下一轮）
- `appcontentholder appnavigation.rich` 的 3 行网格（`23px 39px 25px`）
  与背景 `linear-gradient(to right, #f4f4ee, #e0e2eb)`
  —— 我们的工具栏/地址栏**结构是对的**，但用的是作者手挑的颜色，不是这组精确值
- `css/applayouts.css`（55KB）里各应用的内部布局

### 本轮取到的基准调色板（后续对齐直接查这里）`--mainColor: #ece9d8`（窗口/对话框底色）、`--mainText: #000`、
`--3dHighlight: #fff`、`--3dLightShade: #aca899`、`--3dDarkShade: #716f64`、
`--highlightColor: #316ac5`、`--highlightText: #fff`、
`--menuColor: #fff`、`--menuText: #000`、
`--contentColor: #fff`、`--contentText: #000`、
`--activePrimary: #0054e3`、`--activeSecondary: #3d95ff`、
`--inactivePrimary: #7a96df`、`--inactiveSecondary: #9db9eb`、
`--desktop: #004e98`、`--internalBackdrop: #808080`

---

## 资源管理器导航区对齐（**未提交**）

### 基准规格 `appnavigation.rich`（`css/luna/modules.css`）
```css
appcontentholder appnavigation.rich {
    background: linear-gradient(to right, #f4f4ee, #e0e2eb);
    display: grid;
    grid-template-columns: 100%;
    grid-template-rows: 23px 39px 25px;      /* 菜单行 / 工具行 / 地址行 */
}
appcontentholder appnavigation ul {
    width: calc(100% - 2px); padding-left: 2px;
    border-bottom: 1px solid #d8d2bd;         /* 每行下边线 */
}
appcontentholder appnavigation ul:nth-of-type(n+2) {
    border-top: 1px solid #fff;               /* 第二行起还有上白线 */
}
appcontentholder appnavigation navflag {      /* 右上角的 IE 小旗 */
    position: absolute; right: 0; top: 0;
    width: 39px; height: 22px;
    background: url("../../res/ui/browserflag.png");
    border-width: 0 0 1px 1px; border-color: #d8d2bd; border-style: solid;
}
```

### 我们的结构与差异
`MyComputer` 的三段式结构（`.com__toolbar` / `.com__function_bar` / `.com__address_bar`）
**本来就和基准的三行对应**，但用的是作者手挑的颜色：
`rgba(255,255,255,.7)` / `rgba(0,0,0,.1)`，并且**完全没有那条浅色背景渐变**。

### 本轮改动
1. 三个导航行都加上 `background: linear-gradient(to right, #f4f4ee, #e0e2eb)`
   （渐变是横向的，所以逐行加与加在父容器上视觉一致，无需改 DOM）
2. 分隔线统一为**上白下灰**：`border-top: 1px solid #fff` +
   `border-bottom: 1px solid #d8d2bd`
3. 右上角标志由 `windowsIcons/windows.png` 换成基准指定的 `ui/browserflag.png`
   （两者都是 39×22，但后者带透明通道、质量更好），边框改为 `#d8d2bd`
4. 移除了地址栏上基准没有的 `box-shadow: inset 0 -2px 3px -1px #b0b0b0`
5. `.com__options` 原本重复画了一条内部分隔线，去掉，交给行的 border

**确认过不会出现双线**：`.com__content` 是 `border: 1px solid rgba(0,0,0,.4)`
配 `border-top-width: 0`，地址栏的下边线不会与它叠成两条。

### 仍未对齐（下一步）
- 工具栏按钮的边框（`index.js` 约 951-961 行）仍是
  `rgba(0,0,0,.1)` / `rgba(255,255,255,.7)`，但那是**按钮态**样式，
  不是分隔线，不能简单换色，要对齐得看 `controls.css`
- 地址栏里"转到"前的竖分隔条（约 1107-1108 行）同上
- 左侧边栏（`.com__sidebar__group` 等，1138 行起）在基准里属
  `appcontentholder appnavigation` 的另一套规则，尚未核对
- `MyComputer/index.js` **导入了大量 `windowsIcons/svg/*.svg`**（含 >500KB 的
  `My Computer.svg` 等），是构建期 Babel 大文件降级提示的来源之一；
  未擅自替换（换图标会改变外观）

---

## ⚠️ 事故与回退：窗口按钮被我改坏（2026-09-15 晚）

### 用户反馈
"任何物品都是只实现了按钮对应效果全部失败不能使用，所有窗口右上角的按钮甚至还是错误的"
并附了「我的电脑」标题栏截图——右侧三个按钮变成了白方块。

### 根因（已查明，是我的实现错误）
`16a0406` 那轮我把窗口按钮的图标从 **CSS 绘制**改成了**真实位图**
（`res/ui/luna/{close,maximize,minimize}.png`），并用了 `background-size: contain`。

但那三张位图是 **256×256 的画布，图标本身只占极小一块**：

| 文件 | 尺寸 | 不透明像素 | 占画布 |
|---|---|---|---|
| `close.png` | 256×256 | 336 | 0.51% |
| `maximize.png` | 256×256 | **30** | 0.05% |
| `minimize.png` | 256×256 | **15** | 0.02% |

`contain` 把整张 256px 画布塞进 19px 的按钮 → 图标被缩到 **约 1 像素**，等于看不见，
只剩下我设的渐变底色，所以看起来就是白方块。

**教训：从参考站搬位图之前，必须先确认位图里的实际图形占多大比例。**
不能只看文件名和尺寸就当成图标用。

### 已完成的回退（改 `HeaderButtons.js` + `index.css` + `WinXP/index.js`）
- `HeaderButtons.js` 已 **diff 确认与 `16a0406^` 完全一致**（回到 CSS 绘制方案）
- `index.css` 里 `--winbtn-*` 的取值全部还原为改动前的值
  （`inset 0 -1px 2px 1px #4646ff` 等），并删除本轮新增的
  `--winbtn-size` / `--winbtn-shadow-hover` / `--winbtn-*-bg-hover` / `--winbtn-base-color`
- `WinXP/index.js` 移除 6 个位图 import、`themeImageVars` 与 `style={themeImageVars}`
- 校验：CSS 变量 42 定义 / 42 引用，无未定义、无死变量；已无 JS 注入变量

**注意：窗口框架/标题栏那部分对齐（`16a0406` 的其余内容）未回退**，
用户抱怨的是按钮，框架部分保留。

### 仍未查清的：用户提到的"全部失败不能使用"
这个描述比较宽泛，未能定位到具体是哪几个应用、什么操作失败。
新加的应用（命令提示符/音量控制/计算器/写字板/字符映射表/图片查看器/三维弹球）
**都只做过静态校验，从未真正运行过**，因此存在运行时 bug 的可能。
**下一步应当实际起开发服务器跑一遍**（而不是继续盲改），
用浏览器工具打开真实界面复现，才能定位"不能使用"到底指什么。

---

## ✅ 三维弹球崩溃已修复并实测通过（2026-09-15 晚）

### 用户提供的控制台报错（关键线索）
```
3DPinballSpaceCadet.js:1316 Uncaught ReferenceError: GAME_SOUND_ENABLED is not defined
  at 3405644 → _emscripten_asm_const_int → Module._main → callMain → doRun → run
```

### 根因
WASM 的 `main()` 会**直接读取一个 JS 全局变量**（经 `emscripten_asm_const_int` 调用），
源码就是（`3DPinballSpaceCadet.js` 第 1316 行附近）：
```js
if (!GAME_SOUND_ENABLED) { Module.SDL2.audioContext.suspend(); }
else                     { Module.SDL2.audioContext.resume(); }
```
这个全局由**参考站的宿主页面定义**（用 cookie 持久化），**我写宿主页时漏了**，
所以游戏一启动就抛 ReferenceError 直接挂掉。

### 修复
在 `public/pinball/index.html` 的 `<script>` 里、加载 WASM 之前定义：
```js
var SOUND_STORAGE_KEY = 'pinballspacecadet.GAME_SOUND_ENABLED';
var GAME_SOUND_ENABLED = (function () {
  try { return localStorage.getItem(SOUND_STORAGE_KEY) !== 'false'; }
  catch (e) { return true; }
})();
```
并让右上角的音乐开关同步这个全局 + 持久化。
顺带删掉 iframe 上的 `allowFullScreen`（与 `allow="fullscreen..."` 重复，
React 会警告 `Allow attribute will take precedence over 'allowfullscreen'`）。

### 实测验证方法（**很有用，记下来**）
开发服务器**不可用**：`npx react-app-rewired start` 跑了 8 分钟仍是转圈、
端口 3000 一直不监听 —— 因为 `src/assets/windowsIcons/svg` 有 187MB，
webpack 处理量过大。**不要靠 dev server 验证**。

正确做法：**弹球是纯静态自包含的**，直接起静态服务验证即可：
```bash
python3 -m http.server 8902 --bind 127.0.0.1 --directory <项目>/public
agent-browser open http://127.0.0.1:8902/pinball/index.html
```
实测结果（用 `agent-browser eval` 取）：
- `gameSoundDefined: "boolean"`、`value: true` → 全局已定义
- `canvas: 1280x633`、`ctxType: WebGLRenderingContext` → 画布已初始化并渲染
- `splashHidden: true` → `onRuntimeInitialized` 已触发
- 截图确认：完整的 Space Cadet 台面、`BALL 1`、`Player 1`、`Awaiting Deployment`、
  顶部游戏自带 Game/Options/Help 菜单栏

### 顺带澄清：用户看到的另两条报错与本项目无关
`all.js` / `content.js` 里的 `Error with Permissions-Policy header: Unrecognized
feature: 'attribution-reporting'` 与 `[KISS-Translator] [INFO] ...`
来自**浏览器的翻译扩展**（KISS-Translator），不是项目代码。
以后排查只看 `main.js` / `3DPinballSpaceCadet.js` 等本项目文件的报错即可。

---

## ⚠️ 系统性缺陷：11 处右键菜单项点了毫无反应（2026-09-15 晚，已修复）

### 用户反馈
"主要问题出现在右键这些"、"只实现了按钮对应效果，全部失败不能使用"。

### 根因：把 React onClick 写在了 `<contextmenu>` 里
本项目的右键菜单由全局处理器 **`cloneNode(true)` 克隆**后挂到容器上，
**克隆节点会丢失 React 的事件处理器**。所以写在 `<contextmenu>` 内部的
`onClick` 全部静默失效——**表现为菜单能弹出、项看着能点，但点了菜单都不关、
什么也不发生**（因为 `menu.remove()` 就在那个失效的点击回调里）。

代价：全库 **11 处**，分布在 3 个文件——
- `Windows/index.js` 3 处（窗口标题栏菜单：最小化 / 最大化 / 关闭）
- `apps/MyComputer/index.js` 2 处（外链「打开」）
- `apps/Notepad/index.js` 6 处（剪切 / 复制 / 粘贴 / 删除 / 全选 / 时间日期）

**这条坑其实早就写在项目记忆里，我却在写新代码时又犯了。**

### 修复
给全局处理器补齐两类通用机制，然后把 11 处全部改成 `data-*`：
1. `data-url` —— 直接开外链（`window.open(url,'_blank','noreferrer')`）
2. `data-cmd` —— 派发 `window` 上的 `CustomEvent('winxp:menu-command', { detail:{ cmd } })`，
   由应用自己监听处理（适合应用专属命令，如记事本的"时间/日期"）
处理器的选择器相应扩为
`'[data-action], [data-vfs], [data-app], [data-cmd], [data-url]'`。

改动点：
- `Windows/index.js`：改为 `data-action="restore|minimize|maximize|close"` + `data-win-id={id}`；
  顺带把「还原」由死禁用改为"最大化时才可用"，接了全局处理器已支持的 `restore`
- `Notepad/index.js`：改用 `data-cmd`，并新增 `useEffect` 监听 `winxp:menu-command`；
  **粘贴保持禁用**（浏览器禁止脚本主动粘贴，不能留个点了没反应的项）
- `MyComputer/index.js`：两处外链「打开」改用 `data-url`
- `WinXP/index.js`：桌面菜单的「属性」由 React onClick 改为 `data-app="DisplayProperties"`

### 排查方法（可复用，已实测有效）
1. **静态扫描**：正则切出所有 `<contextmenu>…</contextmenu>` 块，查里面有没有 `onClick=`
2. **运行时扫描**（更可靠）：遍历所有 `contextmenu` 模板，找出
   **"没有 disabled 但没有 data-action/vfs/app/cmd/url"** 的 `li`
   —— 这些就是"看着能点、点了没反应"的项
3. **功能验证**：先派发 `contextmenu` 事件让菜单弹出，再对目标 `li` 调 `.click()`，
   比较前后「可见菜单数」与「窗口数」。**能用的项点完菜单会关闭（可见菜单数 0），
   失效的项菜单会一直开着**——这个判据很灵。

### ⚠️ 扫描时的假通过陷阱
第一次复扫报"0 个问题项"，其实是**假通过**——当时页面还停在登录界面，
桌面菜单模板根本没渲染，扫了个空。
**扫描前必须先断言目标模板存在**（`!!document.querySelector('contextmenu[data-desktop-menu]')`），
否则会把"没渲染"误判成"没问题"。

### 实测结果
点击桌面右键「属性」：窗口数 0 → 1，菜单正常关闭，新窗口标题 **「显示 属性」**。
修复前：菜单不关、窗口数不变。

### 开发服务器（用户要求保持运行）
`npx react-app-rewired start` 已启动在 **端口 3000**，**用户要求在他没说关闭前不要关**。
首次编译约 8-10 分钟（`src/assets/windowsIcons/svg` 有 187MB）。
以后需要验证界面时直接用它，配合 `agent-browser` 操作
`http://127.0.0.1:3000/`；登录界面点 `Administrator`（snapshot 里的 `@e2`）进桌面。
**注意：每次 `agent-browser open` 重载后 ref 会失效，需要重新 snapshot。**
更稳的登录方式：`document.querySelector('.user').click()`（.user 是登录项的可点元素）。

---

## ⚠️ 任务栏右键菜单：一块死菜单（2026-09-15 晚，已修复）

### 关键结构：任务栏有 **4 个独立的 contextmenu 块**（`Footer/index.js`）
| 块 | 宿主元素 | 用途 |
|---|---|---|
| #1 | `div.footer__items.left` | 任务栏**空白处**右键 |
| #2 | `div[data-contextmenu][style*="inline-flex"]` | **开始按钮**右键 |
| #3 | `div.footer__items.right` | **托盘区/时钟**右键 |
| #4 | `div[data-contextmenu][data-win-id]` | 任务栏**窗口按钮**右键 |

### 用户反馈"右键任务栏显示桌面还是没有用"——两个真实 bug
1. **块 #3（托盘区）**：`<li className="disabled">显示桌面</li>` ——
   **既 disabled 又没有 data-action**，点了毫无反应。块 #1 里的同名项却是好的
   （`data-action="show-desktop"`），典型的复制粘贴漏改。
2. **块 #2（开始按钮）**：整个菜单 **8 项全是 disabled** 的占位内容
   （打开/资源管理器/搜索/属性/打开所有用户…），**而且根本没有「显示桌面」**。
   XP 里开始按钮右键应与任务栏空白处弹同一个菜单。

### 修复
- 块 #3 的显示桌面 → `<li data-action="show-desktop">`
- 块 #2 整体替换为与块 #1 一致的任务栏菜单（工具栏/层叠窗口/横向平铺/纵向平铺/
  显示桌面/任务管理器/锁定任务栏/属性），其中显示桌面接了 `data-action="show-desktop"`

### 实测
点托盘区菜单的「显示桌面」→ 窗口 `展示中: false`（已隐藏）、
任务栏按钮从 `footer__window focus` 变回 `footer__window cover`。✅

---

## 「新建」实测**是通的**（未能复现用户的失败）

用户反馈"新建操作还是完全没有实现"。我做了完整链路实测，**全部正常**：

| 步骤 | 实测结果 |
|---|---|
| 悬停「新建」展开子菜单 | ✅ 用 `agent-browser hover` 验证：子菜单 `display` 由 `none` → `block`，4 个子项 |
| 点击「文件夹」 | ✅ 菜单关闭、无报错 |
| VFS 是否真的创建 | ✅ localStorage `winxp.vfs` 里 Desktop 目录多出 `新建文件夹` |
| 桌面是否显示图标 | ✅ 图标渲染出来且可见（top 535，视口 633） |
| 刷新后是否还在 | ✅ VFS 与桌面图标都保留 |

**线上 bundle 也确认含有这些代码**（`SYNC_DESKTOP_ICONS`、`arrange-name`、
`ImageViewer`、`Pinball` 均在 main.js 里），**不是"线上还是旧版本"导致的**。
→ 已请用户补充具体操作步骤与现象（尤其：悬停「新建」时子菜单有没有弹出来）。

### 排查这类问题的方法（重要）
- **`agent-browser hover` 是验证子菜单的关键**。程序化 `.click()` 会**绕过 CSS `:hover`**，
  所以只测 click 无法证明"用户能点到子菜单项"——第一次我就是这么误判为"功能正常"的
- 判断 VFS 操作是否成功，**直接读 `localStorage.getItem('winxp.vfs')`** 最可靠，
  比在 DOM 里找图标元素确定得多
- 判断窗口是否真被最小化，看**外层元素的 computedStyle.display** 与
  任务栏按钮的 class（`focus` / `cover`），比看截图稳

---

## ✅ 「新建点击后不显示」真正根因：**子菜单跑到屏幕外**（已修复）

### 用户反馈
"桌面和我的电脑的新建点击后都不显示"，且**确认是在本地 dev server 上测的**。

### 为什么我前两次都测不出来
我一直在**屏幕中间**右键（clientX 420~500），子菜单有足够空间向右展开，所以一切正常。
用户是在**靠右**的位置右键 → 子菜单整个渲染到视口外 → 看不到、点不到 → "不显示"。

### 实测数据（视口宽 1280，在 x=1150 处右键）
| | left | right | 是否溢出 |
|---|---|---|---|
| 主菜单 | 1118 | 1278 | 否（**已做边界收敛**）|
| **子菜单**（排列图标 / 新建） | 1275 | **1435** | **是，超出右边界 155px** ❌ |

**根因**：主菜单有 `Math.min(clientX, innerWidth - mw - 2)` 的收敛逻辑，
但子菜单是 `contextmenu > ul ul { left: 100% }` **死板向右展开，没有任何溢出处理**。

### 修复
1. `WinXP/index.js` 的全局右键处理器：克隆出菜单并定位后，遍历
   `li.submenuholder`，**临时 `display:block + visibility:hidden` 量出子菜单宽度**
   （因为平时是 `display:none` 量不到），若
   `holder.getBoundingClientRect().right + 子菜单宽 > innerWidth` 则加上 `submenu-left` 类
2. `index.css` 新增：
   ```css
   contextmenu > ul li.submenuholder.submenu-left > ul {
     left: auto;
     right: 100%;
   }
   ```

### 验证（两个方向都测了）
| 右键位置 | flipLeft | 子菜单位置 | 结果 |
|---|---|---|---|
| 靠右 x=1150 | `true` | left 961 → right 1121 | 向左展开，不溢出 ✅ |
| 靠左 x=200 | `false` | left 357 → right 517 | 仍向右展开，行为不变 ✅ |

### 教训
**测试位置要覆盖边界**。只在屏幕中间测，就永远发现不了"靠边溢出"这类问题。
另外：主菜单做了收敛而子菜单没做，这种**同一机制只做了一半**的情况很容易漏。













## 开始菜单"有入口无分支"清单（重要，可当补应用的路线图）

扫描方法：把 `FooterMenuData.js` 里所有 `type:'item'` 的 `text` 与
`WinXP/index.js` 中 `onClickMenuItem` 的 `o === '...'` 分支做差集。
注意**分组标题**（附件/游戏/娱乐/系统工具等）是带嵌套 `items` 的容器，
不应用「单层花括号」正则去匹配，否则会被误判成可点击条目。

已接分支（11）：Internet / Internet Explorer / Windows 资源管理器 / 扫雷 /
我的电脑 / 记事本 / Winamp / 画图 / 命令提示符 / 音量控制 / 计算器
（另加 注销 / 关闭计算机 / 关于 Windows 等系统项）

仍未接分支的条目（约 46 个），按"是否已有可复用实现"分组：

**有素材、可直接新写（推荐优先）**
- 写字板（wordpad 图标已有）、录音机、磁盘清理（diskclean）、系统信息、
  字符映射表（font）、磁盘碎片整理程序、Windows Media Player（wmp 素材已移植）

**游戏类（需要较多实现）**
- 纸牌、蜘蛛纸牌（spider 图标已有）、空当接龙、红心大战、三维弹球（见 MEMORY.md 未来工作）

**向导类（内容型，成本低但价值一般）**
- 辅助功能向导、网络安装向导、新建连接向导、无线网络安装向导、
  文件和设置转移向导、程序兼容性向导、漫游 Windows XP、远程协助、同步

**系统工具类（偏后端，收益低）**
- 备份、计划任务、安全中心、系统还原、远程桌面连接、Windows Update、
  显示所有连接、网络连接、超级终端、通讯簿、设定程序访问和默认值、Windows 目录

**未移植的第三方/微软组件**
- Outlook Express、Windows Messenger、Windows Movie Maker、MSN、
  Internet 系列小游戏（双陆棋/跳棋/红心大战/黑白棋/拱猪）

（待办清单见本文件顶部「本轮追加」小节，此处不再重复）

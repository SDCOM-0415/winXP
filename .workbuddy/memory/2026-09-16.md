# 2026-09-16 工作日志

## 1. 修复：右键子菜单在屏幕右侧溢出（靠边右键时「新建」看不到）

### 用户反馈
"桌面和我的电脑的新建点击后都不显示"，且确认是在本地 dev server 上测试。

### 根因
主菜单做了边界收敛（`Math.min(clientX, innerWidth - mw - 2)`），
**但子菜单是 `contextmenu > ul ul { left: 100% }` 死板向右展开，没有任何溢出处理**。

实测（视口 1280，在 x=1150 处右键）：

| | left | right | 溢出 |
|---|---|---|---|
| 主菜单 | 1118 | 1278 | 否（已收敛）|
| **子菜单**（排列图标 / 新建） | 1275 | **1435** | **是，超出右边界 155px** |

子菜单整个渲染到视口外 → 用户看不到、点不到 → 表现为"新建不显示"。

**我为什么前两次测不出来**：一直点在屏幕中间（x≈420~500），子菜单有空间向右展开。

### 修复
1. `WinXP/index.js` 全局右键处理器：克隆菜单并定位后，遍历 `li.submenuholder`，
   **临时 `display:block + visibility:hidden` 量出子菜单宽度**
   （平时是 `display:none` 量不到宽度），若
   `holder.getBoundingClientRect().right + 子菜单宽 > window.innerWidth`
   则加上 `submenu-left` 类
2. `index.css` 新增：
   ```css
   contextmenu > ul li.submenuholder.submenu-left > ul {
     left: auto;
     right: 100%;
   }
   ```

### 验证（两个方向都测）
| 右键位置 | flipLeft | 子菜单位置 | 结果 |
|---|---|---|---|
| 靠右 x=1150 | `true` | left 961 → right 1121 | 向左展开，不溢出 ✅ |
| 靠左 x=200 | `false` | left 357 → right 517 | 仍向右展开，行为不变 ✅ |

### 未做但已知的同类问题
子菜单的 `top: -3px` **也没做垂直溢出处理**——在屏幕底部右键时子菜单可能往下方超出。
已告知用户，待其确认是否遇到。

---

## 2. ⚠️ 重大教训：dev server 编译失败 + HMR 断连 = 用户看到的是旧代码

用户后续反馈"即使在桌面上点击右键以后还是什么都没有"。排查后确认**代码是好的**，
问题出在验证与交付环节。

### 真相（查 dev server 输出日志才看到）
1. **我一边改一边报错**，`react-app-rewired start` 日志里有一连串：
   ```
   ERROR in [eslint]  src/WinXP/apps/Notepad/index.js Line 186:17  prettier/prettier
   webpack compiled with 1 error and 1 warning
   ```
   **webpack 编译失败时会继续提供上一次成功的 bundle**，页面不会自动切到新代码
2. 用户控制台里有：
   ```
   WebSocket connection to 'ws://localhost:3000/ws' failed: Page entered Back-Forward Cache.
   ```
   **HMR 连接已断**，后续所有修复都没能推送过去
3. 我"恰好"在编译成功的时刻测试，所以看到的是好的 → 错误地宣称"我这边正常"

### 必须遵守的流程
- **改完一批后先确认 dev server 编译成功**（`TaskOutput` 里要有
  `Compiled successfully` / `Compiled with warnings`；出现 `Failed to compile`
  就说明页面还是旧代码），再去下结论
- **告诉用户硬刷新（Ctrl+Shift+R）**，不要指望 HMR
- **不要用"我这边测了没问题"来回答用户**，先确认双方跑的是同一份代码

---

## 3. 关于 `<contextmenu>` 的 React 警告（已向用户说明，暂不改）

```
Warning: The tag <contextmenu> is unrecognized in this browser.
in contextmenu (at Icons/index.js:215)
```

**纯提示，不影响功能**。React 16 对非标准 HTML 标签会打这条警告，
但仍照常渲染成自定义元素。已用实测反证它**不是**"右键没反应"的原因：
菜单能弹出 21 项、子菜单能展开、点击能创建文件夹。

要消除它只能把标签改名（`<contextmenu>` → `<div className="contextmenu">`），
需改约 25 条 CSS 选择器（`contextmenu ul li …` 等）与十几处
JSX / `querySelector('contextmenu')` 查询。
**为一条控制台提示去动这么大范围、且引入回归风险，判断不划算** ——
已把利弊交给用户决定。

---

## 4. 用户控制台里与本项目无关的噪音（可忽略）
- `all.js` / `content.js` / `[KISS-Translator]` —— 浏览器**翻译扩展**发出的
- `[Intervention] Slow network ... fonts.gstatic.com/...notosanstc` —— Google Fonts 加载慢
- `<meta name="apple-mobile-web-app-capable"> is deprecated` —— 来自 public/index.html
- `WebSocket ... /ws failed` —— HMR 连接（见第 2 条，这条**要重视**）

---

## 6. 🎯 真正的根因：`onMouseDownDesktop` 无条件删除右键菜单（已修复并真实鼠标验证）

### 用户反馈
"依旧没有任何作用，而且这不是右键菜单的问题，是完全没有任何新建效果生效"

### 根因（用 `Element.prototype.remove` 打桩抓出的调用栈）
```
Element.remove ← HTMLDocument.dismiss (main.js:20054)
              ← onMouseDownDesktop (main.js:20159)  ← React callCallback
```

`WinXP/index.js` 里：
```js
function onMouseDownDesktop(e) {
  document.querySelector('contextmenu.visible')?.remove();   // ← 罪魁祸首
  ...
}
```
右键菜单是 `appendChild` 到该容器**内部**的，所以**点在菜单项上的 mousedown 会冒泡到这里**，
菜单被无条件删掉。事件序列实测：

```
mousedown target=LI  li=文件夹  inMenu=true    ← 按在菜单项上
mouseup   target=DIV inMenu=false              ← 抬起时菜单已经没了
```

**菜单在 mousedown 到 mouseup 之间消失 → 浏览器判定"不是一次完整点击"→
`click` 事件永不触发 → 所有菜单项的处理器都不执行。**

这解释了用户最初的"所有东西都只实现了按钮效果、实际全部失败"：
**任何右键菜单项在真实鼠标下都点不动**。

### 为什么我之前所有测试都"通过"了
我一直用 JS 直接调 `el.click()`。**程序化 `.click()` 不会触发 mousedown**，
所以绕过了这个 bug。这是本次排查最大的方法错误。

### 修复
```js
const openMenu = document.querySelector('contextmenu.visible');
if (openMenu && openMenu.contains(e.target)) return;   // 点在菜单内，交给菜单自己处理
openMenu?.remove();
```
（全库只有两处删除菜单：另一处在 `contextmenu` 事件里，作用是开新菜单前关旧的，逻辑正确。）

### 验证（**全程真实鼠标输入** `agent-browser mouse move/down/up`）
| 场景 | 结果 |
|---|---|
| 桌面：真实右键 → 悬停新建 → 真实点文件夹 | Desktop 条目 4 → **5**，桌面出现「新建文件夹」✅ |
| 我的电脑：进 C 盘 → 真实右键 → 悬停 → 真实点文件夹 | 列表 4 → **5**，「新建文件夹」出现在首行 ✅ |

### ⚠️ 方法论（必须记住）
**验证交互功能必须用真实输入**，不能用程序化 `.click()` / `dispatchEvent`。
`agent-browser` 的真实输入方式：
```bash
agent-browser mouse move <x> <y>
agent-browser mouse down left      # 或 right
agent-browser mouse up left
```
定位元素坐标用 `getBoundingClientRect()` 算出中心点再 `mouse move` 过去。
**这次就是靠真实输入才复现出用户的问题**——此前三轮"我这边正常"全是假阳性。

### 另一个可复用的排查技巧
给 `Element.prototype.remove` 和 `Node.prototype.removeChild` 打桩，
在元素被删时记录 `new Error().stack`，能**直接点名是哪段代码删的**。
本次正是靠这个从"菜单莫名其妙消失"一步定位到 `onMouseDownDesktop`。

---

## 7. 按用户批评重做：音量推子与就地重命名（已验证）

### 用户批评（完全成立）
"你的创建策略还是修改名称策略都太假了，包括音量控制台，能够直接用浏览器的默认控制条
这完全不符合真实windowsxp，然后就是重命名，谁告诉你要用浏览器提示框的？
Windows都是在文件名那里的提示框修改，你要是不知道怎么做是正确的你好歹查阅一下
真正的Windows和参考网站怎么做的啊"

**我该一开始就查参考站，而不是用 window.prompt / 原生 range 凑合。**

### 从参考站查到的规格（爬取副本）
1. **内联重命名**（`css/icons.css` 的 `[contenteditable=true]`）：
   白底 #fff、黑字、1px solid #000、无文字阴影、`user-select:text`、
   width auto、min-width 60px、max-width 150px、padding 1px 2px
2. **音量滑块**（`css/luna/controls.css` 的 `input[type=range].vertical.segmented`）：
   宽 30px；轨道 `repeating-linear-gradient`（两侧边栏 + 每 11px 一道刻度线）；
   滑块 inset 阴影做绿色 3D 高光（#48cb46/#1fae1d），悬停变橙（#fac158/#e2a330）
3. **音量控制布局**（`css/applayouts.css` 的 `appcontentholder.sndvol32`）：
   菜单栏 + `volcontents`（flex 行，多个通道并排）+ `statusbar`；
   每通道 = 标题 + 静音复选框 + `input.vertical.segmented`

### 改动
1. **音量控制**（`apps/VolumeControl/index.js`）：弃用「原生 range 旋转 90° + accent-color」，
   改为 `writing-mode: vertical-lr; appearance:none` + 上述轨道/滑块样式
   （截图确认：竖向绿色推子 + 刻度凹槽，五通道并排，已是 XP 的样子）
2. **就地重命名**：
   - 全局处理器的 `rename` 分支不再弹 `window.prompt`，改为广播
     `winxp:begin-rename` 事件（`detail: {name, driveId, segments}`）
   - `Icons/index.js`：监听该事件，匹配的图标**就地渲染 `<input>`**
     （白底黑边，按上述规格），Enter 提交 / Esc 取消 / 失焦提交；
     提交回调由 `WinXP/index.js` 传入（负责重名校验 + `VFS_RENAME`）
   - `apps/MyComputer/index.js`：同样监听事件并就地编辑；重名报错改用
     **ErrorBox**（`window.postMessage({type:'open-app',app:'ErrorBox',...})`），不再用 alert
3. 防重复提交：Enter 提交后用 ref 标记，避免随后的 blur 再提交一次
   （否则改名成功后 blur 会把"新名字"当成重名报错）

### 验证（**全程真实鼠标 + 真实键盘**）
| 场景 | 结果 |
|---|---|
| 桌面：真实右键图标 → 重命名 → 输入框就地出现 | ✅ 值=原文件名、已聚焦、全选、白底黑边 |
| 真实键盘输入「测试改名」+ Enter | ✅ VFS 与桌面标签同步更新 |
| 我的电脑：C 盘 BOOT.INI → 重命名 → 输入框出现 | ✅ |
| 真实键盘输入「BOOT测试」+ Enter | ✅ 界面列表与 VFS 都变更为 BOOT测试 |

### 仍用浏览器提示框的地方（已知，未改）
- `Notepad/index.js` 56/75 行、`WordPad/index.js` 113/132 行的「另存为/打开」
  —— 正确做法是做一个 XP 风格的文件对话框（树 + 列表 + 文件名输入框），工作量较大
- 「新建文件夹」创建后应**立即进入重命名态**（XP 如此），尚未实现

### 附带澄清（排查中一度误判）
- 内置应用图标（我的电脑/记事本/扫雷/画图/Winamp/更新日志.md）**没有 vfsPath**，
  它们的「重命名/删除」本来就该禁用 —— 这与真实 XP 一致，不是 bug
- 排查时我的 DOM 查询两次选错元素（把窗口当图标、把菜单 `<li>` 的属性当成图标根元素的），
  得出"vfsPath 全为空"的错误结论。**判断要选对元素、用菜单项自身的 disabled 状态佐证**

---

## 8. 新建的三个问题：网格布局（已修）＋ 立即重命名（回退，待重做）

### 用户反馈
"创建文件默认修改文件名没有实现，而且在修改完成名称后会自动往下一格，
而且文件创建是在右键区域而不是默认位置新建"

### 实测确认的三个问题
1. **新图标排到屏幕外**：位置分配是「无限往首列下方追加」（`y: index * 75`），
   7 个内置图标之后新图标落在 y=610、685、985…全部超出 633px 视口。
   用户看到的就是"什么都没出现"
2. **创建后没有进入重命名态**：确认未实现（这轮做了又回退，见下）
3. **重命名后图标跳位**：重命名改变图标 id（`vfs:旧名 → vfs:新名`），
   位置表里没有新 id，被当成新图标排到下一个空位

### 已修复（真实鼠标验证通过）
1. **网格布局 + 换列**：`getInitialPositions` 与"补位"effect 都改为
   80×75 的网格，`rows = floor((innerHeight - 40) / 75)`，一列满则换列。
   实测：16 个图标（含新建的 (8).txt）**全部可见，零溢出**
2. **重命名后保持原位**：给 `Icons` 增加 `migratePosition(oldId, newId)`
   的命令式方法，`onRenameDesktopIcon` 在派发 `VFS_RENAME` 后调用
   （图标 id 格式是 `vfs:${name}`）

### ⚠️ 回退：「新建后立即重命名」引发了更糟的回归
第一版实现：创建的 dispatch 后**立刻** `setPendingRename(...)` 广播事件。
结果实测发现 **`VFS_CREATE_FILE` 之后文件没有真正落进 VFS**
（插桩显示：创建分支执行了、`setPendingRename` 生效了、effect 也跑了、
但 VFS 里没有新文件，且事件从未广播）。

由于无法在剩余时间内定位这个回归，**已把该部分整体回退**，
回退后创建功能恢复正常（真实鼠标验证 (7)(8) 都能创建）。
**结论：这个功能需要重新设计，不能在同一个 click 回调里
"dispatch 创建 + 依赖同步后的图标列表"两件事混在一起做。**
可能的方向：让 Icons 组件直接以 prop 接收"待重命名目标"，
在它自己的 render 里（icons 一定是新的）匹配；或者由 reducer 一次性
完成"创建 + 标记待重命名"。

### 验证方式（可复用）
- 图标可见性：`getBoundingClientRect()` 逐个比对 `window.innerHeight`
- 创建是否成功：读 `localStorage.getItem('winxp.vfs')` 数条目，**别只看 DOM**
- 全程 `agent-browser mouse move/down/up` 真实输入

### 当前未提交改动一览（全部经真实输入验证）
- 窗口按钮回退（HeaderButtons + winbtn 变量）
- 弹球修复（GAME_SOUND_ENABLED）
- 右键菜单 data-* 化（11 处失效的 onClick）
- 任务栏三块菜单（托盘显示桌面、开始按钮菜单）
- 子菜单水平溢出翻转（submenu-left）
- 桌面图标网格布局 + 换列、重命名后位置迁移
- 音量控制 XP 竖向推子、桌面/我的电脑就地重命名

---

## 5. 重启开发服务器（用户要求，12:16）

用户要求重启（此前他要求"我不说关闭就不要关"，这次是明确的关闭+重启指令）。

### 操作
1. 确认 3000 端口空闲、无残留 `react-app-rewired` 进程
2. 重新启动：`CI=false BROWSER=none PORT=3000 NODE_OPTIONS=--max-old-space-size=4096 npx react-app-rewired start`
3. 首次编译耗时约 **5 分钟**
4. 新 task_id: `91y0lY`

### 编译结果（这次按新流程专门确认过）
```
Compiled with warnings.
webpack compiled with 1 warning
```
唯一的警告是 `node_modules/webamp/built/webamp.bundle.js.map` 缺失
（依赖自带的老问题，与本项目代码无关）。
**没有 `Failed to compile`、没有 eslint 错误** —— 说明上一轮的
prettier/未使用导入问题已全部清干净。

### 重启后的验证（全部通过）
| 检查项 | 结果 |
|---|---|
| 端口 3000 | HTTP 200 |
| 登录进桌面 | ✅ |
| 桌面右键 | 弹出 **21 项**菜单 ✅ |
| 靠右右键（x=1150）子菜单 | `flipLeft=true`，right=1121，**不溢出** ✅ |
| 靠左右键（x=200）子菜单 | `flipLeft=false`，right=517，行为不变 ✅ |

即：**上一轮的子菜单溢出修复确实已在新 bundle 中生效**。

---

## 9. 音量控制：完整按参考站的方式重写（已验证）

### 用户反馈
"音量设置无论是和参考网站的还是和真实的Windows都不一样，
你需要完全使用参考网站的编写方式"

### 这次把参考站的规格**完整**取了出来（之前只看片段，所以做得不像）
| 来源 | 内容 |
|---|---|
| `css/applayouts.css` | `appcontentholder.sndvol32`：`grid-template-rows: 23px auto 23px`（菜单/通道/状态）；`volcontents` 为 `flex-direction: row` + `overflow-x: auto`；`volentry` 固定 **105px** 宽、`border-right: 1px #aca899` + `box-shadow: 1px 0 #fff`；`volentry form` 为 `grid-template-rows: 30px 24px 1fr 32px`（标题/静音/推子/留白） |
| 同上 | `span.name`：居中、`padding: 8px 0`、`margin: 0 6px`、下边框 + 白色高光、超出省略 |
| 同上 | `input[type=range].vertical`：`width: 24px; height: 100%; -webkit-appearance: none; background: 0 0` + **`transform: scaleY(-1)`**（让 100% 在顶端） |
| 同上 | track：`1px dotted #888`、`border-radius: 0`；凹槽与刻度画在 **track 的 `::before`** 上（`top/bottom: 4px`、宽 8px，`linear-gradient` 出左侧暗线 + 右侧白线，`repeating-linear-gradient` 每 11px 一道刻度） |
| 同上 | thumb：`22×11`、`border-radius: 5px`、`margin-top: -5px`，背景关键在 **右缘一截绿色渐变**（`#78dc63 95%, #58b848 100%`），青色光标 `link.cur` |
| `css/luna/controls.css` | `wincheckbox`：原生 `input[type=checkbox]` 被 `visibility: hidden` 隐藏，**复选框外观完全由 CSS 画**：11×11、`outline: 1px #1c5180`、`border-image: linear-gradient(135deg,#dcdcd7,#fff)`；悬停转橙、按下转深；**勾是两个旋转 45°/-45° 的绿色小方块**（`#21a121`） |
| 同上 | `form li`：`position: relative; padding: 2px 0 2px 18px; height: 16px`，复选框绝对定位在 `left: 0; top: 4px` |

### 改动
1. **结构**：容器改 `grid` 三行；每通道 `<form>` 四行网格；
   `<span class="vc__name">` + `<span class="vc__mute">`（内含隐藏的 input + CSS 画的
   `<span class="vc__checkbox">`）+ `<input class="vc__slider">` + 底部留白
2. **推子**：`writing-mode: vertical-lr` + `transform: scaleY(-1)`；
   `::-webkit-slider-runnable-track` 只留 1px 点线边框，凹槽/刻度用
   `::before` 的两个渐变叠加；`::-webkit-slider-thumb` 复原 22×11 + 右缘绿色渐变
3. **复选框**：隐藏原生 input，用 `:checked ~ .vc__checkbox:after/:before` 显示绿色勾
4. **窗口尺寸**：`defaultSize` 230 → **560**（105px × 5 通道 + 内边距），
   `defaultOffset.x` 由 `innerWidth - 460` 改为 `max(10, innerWidth - 620)`，
   否则 560 宽的窗口会伸出屏幕右侧

### 踩到的两个坑
1. **`.vc__name` 用固定 `height: 13px` 会把标题裁掉**（该项目的 `clear.css`
   似乎全局设了 `box-sizing: border-box`，13px 再去掉上下 8px 内边距就不够）。
   改为 `padding: 8px 0 7px; line-height: 13px`，实测标题框 30px，正常显示
2. **复选框 `left` 写成 18px 是错的**：父元素已有 `padding-left: 18px`，
   绝对定位应取 `left: 0`，否则双重偏移

### 验证
- 结构：5 通道 / 5 复选框 / 5 推子；`form` 行高实测 `30px 24px 136px 32px`
  （对应 `30px 24px 1fr 32px`）
- 窗口 `[664, 1216]`，不再超出 1280 视口；5 个通道全部完整可见
- **截图确认**：与参考站一致的通道列布局、点线轨道 + 刻度、右缘绿色推子、
  CSS 绘制的静音复选框

### 仍未做
「新建后立即重命名」（上一条回退的功能）需要用 prop 方案重做；
记事本/写字板的另存为/打开仍是 `window.prompt`，需做 XP 风格文件对话框。

---

## 10. 尝试"实际体验参考站"（用户建议）：观察台可用，但驱动不了它的 OOBE

### 用户建议（正确方向）
"你不会实际体验原版网页吗？即使是创建一个空的html就ifram参考网站呢？"
——  对，一直在读 CSS 猜，没有真的看原版。

### 做法
1. **参考站可以直接打开**：`https://quenq.com/apps/reborn-xp/`
   （不是 xp.quenq.com —— 那个顶层会跳转）
2. 站点里的模拟器装在 `https://xp.quenq.com/` 的 **跨域 iframe** 里，
   且该 iframe 只有 **848x475**，OOBE 的 Next 按钮被裁在可视区外
3. **自建观察台** `/tmp/ref/harness.html`：一个 1024x1000 的 iframe 装 xp.quenq.com，
   用 `python3 -m http.server 8911` 提供 —— 这样模拟器**完整可见**（`npx` dev server 无关）
4. `agent-browser set viewport 1280 1000`（**工具支持 `set viewport W H`**，
   这条之前不知道，很有用）—— 否则视口只有 572px 高，看不到 OOBE 底部按钮

### 结果与卡点
- ✅ 能完整看到模拟器启动：bootloader 菜单（Start Windows XP / Reinstall）、
  然后进入 OOBE「Welcome to Microsoft Windows」，能看清左下 Back / 右下 Next
- ❌ **OOBE 不响应合成输入**：`agent-browser mouse move/down/up` 点 Next、
  以及 `press Enter`，OOBE 都停在同一页（截图逐字节相同）。
  推测它依赖真实 OS 级事件，或需要先给模拟器文档焦点
- ❌ 因为进不去桌面，**无法打开它的音量控制**来目视比对

### 下次可以试的方向
- 观察台里加 `iframe.focus()` + 先点一次画布再敲键
- 看参考站有没有"跳过 OOBE"的入口（例如 URL 参数、localStorage 标记）
- 或者请用户在自己浏览器里打开参考站的音量控制、截一张图给我对照

### 环境备注
- 观察台的静态服务仍开着：`http://127.0.0.1:8911/harness.html`
- 项目 dev server 仍在 3000（用户要求未获许可不关）

---

## 11. 音量控制按用户截图重做（**结构已对齐**）

### 用户给了参考站音量控制的截图 —— 这张图推翻了我之前的实现
截图显示参考站的音量控制是：
- **单列**（只有 `System`），不是五通道 —— 我按 `volcontents` 做了 5 通道，方向就错了
- 标题 `Volume Control`、菜单 `Options / Help`
- 居中的 `System` + 下划线；左对齐的 `Volume:` 标签
- **一根很细的竖直轨道线**，配一个**小圆角推子**（浅色、上下缘绿边）
- `Mute` 复选框
- 一条分隔线 + 底部声卡型号 `Creative SoundBlaster PCI`

### 改动（`apps/VolumeControl/index.js` 整体重写）
1. 五通道 → **单列「系统」**
2. 布局：`系统`（居中+下划线）/ `音量:` / 推子 / `静音` 复选框，
   底部独立一行 `Creative SoundBlaster PCI`（上方分隔线），窗口 268×320
3. 推子重做：细轨道线 + 小圆角推子（浅色底 + 上下缘绿边 `inset 0 ±2px 0 #48cb46/#1fae1d`）
4. 复选框沿用纯 CSS 绘制（原生 input 隐藏）

### ⚠️ 踩到的关键坑：Chrome 不支持轨道伪元素上的再一层伪元素
参考站 CSS 把凹槽画在 `::-webkit-slider-runnable-track::before` 上，
我照抄了 —— 结果**轨道线完全没渲染**（截图里推子孤零零悬着）。
**Chrome 不允许在 `::-webkit-slider-runnable-track` 这种伪元素上再挂 `::before`。**

改成用 **input 自身的 `background-image` 画一条居中的 2px 竖线**（硬色标渐变），
轨道伪元素保持透明 —— 轨道线立刻就出来了。

### 仍未处理
- **托盘音量面板**：点托盘喇叭弹出的面板里还是**原生 `<input type="range">`**，
  与真实 XP 不符，应换成同一套 XP 推子样式（用户提到"任务栏用的音量也不正确"）。
  素材本身没问题：`assets/ui/tray/sndvol.png` 与参考站**字节完全相同**
- 窗口/菜单文案目前保留中文（`音量控制`/`选项`/`帮助`/`系统`/`音量:`/`静音`），
  参考站是英文。若要对齐成英文，改常量即可

---

## 12. ⚠️ 方法教训：应该走 `web-access` skill，而不是自己起 `agent-browser`

### 用户指出的问题（完全正确）
"为什么不调用 web-access？"

我一直在直接调 `agent-browser`（独立浏览器），而**正确的做法**是用
`web-access` skill 的 **CDP 模式直连用户自己的浏览器**。差别是决定性的：
- `agent-browser` 起的是**全新浏览器**，参考站的模拟器在里面是**全新机器**，
  卡在 OOBE 进不去
- 用户的浏览器里有**他自己的状态**（比如已完成的 OOBE、登录态）

### 本项目的 CDP 连接方式（已验证可用，记下来）
用户的 Chrome 已开启远程调试（端口 **9222**）。启动方式：
```bash
# 只有 Chrome 可用时用 --browser 指定，避免每次都问用户
node "/Users/sdcom/.workbuddy/skills/web-access/scripts/check-deps.mjs" --browser chrome
# 输出 proxy: ready (Chrome) 即成功；之后通过 http://localhost:3456 调用
```
常用接口：`/targets`、`/new`（URL 走 POST body）、`/eval`、`/screenshot`、
`/clickAt`（真实鼠标）、`/info`、`/scroll`、`/close`

### 代理的能力边界（这次踩到的）
- **只有按选择器点击，没有坐标点击接口** —— 所以**跨域 iframe 内部我点不到**
  （参考站的模拟器就在 `xp.quenq.com` 的跨域 iframe 里）
- 跨域 iframe 也**无法 eval 注入**
- 因此：需要操作跨域 iframe 内部时，**请用户在浏览器里手动点**是最快的路径；
  我负责截图 + 从像素里量尺寸/色值

### 参考站的访问事实
- 入口：`https://quenq.com/apps/reborn-xp/`
  （`xp.quenq.com` 顶层打开会跳转；它的模拟器装在跨域 iframe 里）
- **模拟器状态不跨会话持久化**：每次加载都是全新机器，会重新走 OOBE

### ✅ 突破：用"穿透层"给 CDP 补上坐标点击能力（已验证）
代理**只有按选择器点击**，但跨域 iframe 内部选不中元素。解法：
1. **不要导航/刷新**页面（导航会重载 iframe → 模拟器重新装系统，桌面状态丢失！）
2. 用 `/eval` **往已加载页面注入一个透明穿透层**：
   ```js
   const d = document.createElement('div');
   d.id = 'hit';
   d.style.cssText = 'position:absolute;width:2px;height:2px;pointer-events:none';
   document.body.appendChild(d);
   ```
3. 用 `/eval` 把它的 `left/top` 设到目标坐标（`left = x-1, top = y-1`，这样**它的中心正好是 (x,y)**）
4. `/clickAt -d '#hit'` → CDP 在该坐标派发**真实鼠标事件**，因为图层
   `pointer-events:none`，事件**穿透到下层 iframe**，模拟器就收到了

**实测有效**：用这招点击登录界面的 `Administrator`，成功进了桌面。

### 坐标换算
- 用户视口 **1680×808**、DPR 2（截图 3360×1616）
- 我看的缩略图宽 1080 → **CSS 坐标 = 缩略图坐标 × 1.556**
- iframe 铺满视口，所以页面坐标 = 模拟器坐标

### 观察台（`/tmp/ref/`）
- `harness.html`：iframe 铺满 `100vw/100vh`（动态尺寸，用户要求），装 `https://xp.quenq.com/`
- 服务：`python3 -m http.server 8911 --directory /tmp/ref`
- 标签 targetId 存在 `/tmp/ref/tid.txt`

### 未完成
托盘音量图标的**精确坐标没找准**（估的 1563,797 点下去没反应）。
下次可以：直接在缩略图上量更准，或请用户双击托盘图标后我量窗口。

---

## 13. ✅✅ 决定性突破：把参考站入口页本地化，实现"同源可读"

### 用户提示
"是不是有js文件在限制着不能直接访问 xp.quenq.com，如果是，将那个js文件修改然后让本地服务器
也能访问，然后剩余没有做这个限制的js调用原有网站的文件进行测试"

### 查清的事实
1. `https://xp.quenq.com/` 的**内联脚本本身就是限制器**，逻辑是：
   ```js
   if (window === window.top && !IS_ELECTRON && !IS_NATIVE) {
       var host = window.location.hostname;
       if (host !== 'localhost' && host !== '127.0.0.1') { location.replace('https://quenq.com/apps/reborn-xp'); }
   }
   ```
   **它本来就放行 localhost / 127.0.0.1** —— 所以从本地服务打开是通过的，**根本不用改它**
2. 页面里的资源都是**相对路径**（`js/wm.js`、`css/index.css` …）
3. **原站给 JS 返回 `access-control-allow-origin: *`** —— 跨域加载 ES 模块也允许

### 做法（**这是关键技巧**）
```bash
curl -s https://xp.quenq.com/ -o xp-index.html          # 只下载入口页
# 在 <head> 后插入 <base href="https://xp.quenq.com/">
# 写到 /tmp/ref/xp/index.html，由已有的 8911 静态服务提供
```
- **页面本身来自 `http://127.0.0.1:8911` → 同源，我可以随意 eval**
- **资源仍从原站加载**（靠 `<base>`）→ 不用镜像几百个文件

### 实测结果（决定性）
直接把这个本地页作为标签打开后 eval：
```json
{"origin":"http://127.0.0.1:8911","host":"127.0.0.1",
 "有shell":"object","有dm":"object","有explorer":"object"}
```
`window.shell` / `window.dm`(DiskManager) / `window.explorer` / `/`(Explorer)
**全部可访问** —— 意味着我能直接：
- 读 DOM 与 `getComputedStyle`，**量出真实的尺寸与色值**
- **程序化 `.click()` 直接操作**（同源，不受跨域限制，也不需要双击技巧）
- 甚至调用它的内部对象

### ⚠️ 这个方案的代价
本地页的 **origin 与原站不同**（`127.0.0.1:8911` vs `xp.quenq.com`），
所以**不共享原站的 IndexedDB/localStorage** —— 模拟器每次都是全新机器，
要重新装一遍系统（几分钟）。换来的是完全可编程访问，值得。

### 页面结构线索（实测）
`body.className = "classic lunaFallback"`（未识别到 luna 主题，回落到 classic）
body 直接子元素含 `DIV.boot-screen` ×3、`SCENE_HOLDER`、`AUDIO` 等；
启动完成后才会出现 `<taskbar><trayarea>` 等自定义元素。

---

## 14. 🎯 参考站音量控制的**实测数值**（同源直接量出来的，不是猜的）

### 托盘元素（`taskbar trayarea > traycontain > *`）
| 序号 | 标签 | 资源 | 事件 |
|---|---|---|---|
| 0 | IMG | `res/icons/tray/xptour.png` | onclick → `apps.load('xptour')` |
| **1** | **IMG** | **`res/icons/tray/sound.png`** | **ondblclick → `apps.load('sndvol32')`** ← 音量 |
| 2 | IMG | `res/icons/tray/security.png` | ondblclick → `apps.load('tbd')` |
| 3 | SPAN | `#clock` | 显示时间 |

**❗️发现**：参考站托盘音量用的是 **`sound.png`**，而**我们 Footer 用的是 `sndvol.png`** ——
这就是用户说的"任务栏用的音量不正确"。**改一行即可**
（`src/WinXP/Footer/index.js`: `import traySound from 'assets/ui/tray/sndvol.png'` → `sound.png`）

### 音量控制窗口（打开方式：`apps.load('sndvol32')`）
`app.sndvol32_app noresize`，标题 `Volume Control`

**实测尺寸（`getBoundingClientRect` + `getComputedStyle`）**：
| 元素 | 尺寸 | 备注 |
|---|---|---|
| `volentry` | **117 × 221** | 内含 form |
| `form` | **105 × 221** | 与前面从 CSS 读到的 `min/max-width: 105px` 一致 ✅ |
| `span.name`（"System"） | 93 × **30** | 居中，下方有分隔线 |
| `span`（"Volume:"） | 105 × **24** | |
| **`input.vertical.segmented`（推子）** | **24 × 135** | 宽仅 24px、高 135px |
| `li`（静音那一行） | 105 × **20** | |
| `label` | 30 × 13 | |
| 原生 `input`（checkbox） | **0 × 0** | 被完全隐藏 ✅ 与 CSS 一致 |
| `wincheckbox` | **11 × 11** | `border: 2px solid transparent` |

**字体大小 = 13px**（不是我实现里用的 11px！）
各元素 `background-color` 均为透明（`rgba(0,0,0,0)`），底由父级 `#ece9d8` 提供。

### 与我这版实现的差异（待校准）
| 项 | 参考站 | 我的实现 | 需改 |
|---|---|---|---|
| 列宽 | 105px | 未固定 | ✔ |
| 推子 | **24 × 135** | 30px 宽 | ✔ |
| 字号 | **13px** | 11px | ✔ |
| 名称行高 | 30px | 26px | ✔ |
| 音量标签行 | 24px | 20px | ✔ |
| 静音行 | 20px | 自动 | ✔ |
| 托盘图标 | `sound.png` | `sndvol.png` | ✔ |

---

## 15. 参考站任务栏/窗口框架的**实测数值**（同源直接量）

量法：`getBoundingClientRect()` + `getComputedStyle()`，选择器用参考站的自定义元素名。

| 元素 | 选择器 | 实测 |
|---|---|---|
| 开始按钮 | `taskbar startbtn` | **97 × 30** |
| 任务区 | `taskbar taskarea` | padding `0 14px 0 8px` |
| 托盘区 | `taskbar trayarea` | **123 × 30**，padding `0 14px 0 0` |
| 托盘容器 | `taskbar traycontain` | **109 × 30**，padding `0 0 0 10px` |
| 托盘单个图标 | `taskbar traycontain > img` | **18 × 30**，padding `8px 1px 6px 1px`（即图标本体约 18×16） |
| 窗口标题栏 | `appheader` | **高 28**，`font-size: 14px`，padding `5px 4px 2px 5px` |
| 音量控制窗口 | `app.sndvol32_app` | **466 × 301**，`background: rgb(0,33,169)`、`border: 1px solid rgb(0,89,232)` |

### 注意：`taskbar` 本身量到 0×0
因为它是自定义元素、靠内部子元素撑开或用了绝对定位，
**不能只看它自己的盒子，要量它的子元素**（`startbtn` / `taskarea` / `trayarea`）。

### 移交流程提示
用户要求"把不对的组件和做法都完整移植下来"。**正确做法**：
用同源实测拿到**事实数值**（尺寸/颜色/间距），再在**本项目自己的 React 组件里重新实现**——
参考站是原生自定义元素 + CSS，本项目是 React + styled-components，
**不能照抄它的源码**，只能对齐设计数值。

### 待移植清单（按影响排序，等用户排优先级）1. 任务栏各段尺寸（本次已量到，可与现有实现比对）
2. 窗口标题栏（高 28 / 字号 14 / padding 5,4,2,5）
3. 资源管理器导航区（`appnavigation.rich` 的三行）
4. 开始菜单
5. 记事本/写字板的「另存为/打开」——参考站的对话框实现（替换 `window.prompt`）

---

## 16. 窗口右上角三个按钮的**实测数值**（已按此修正）

用户指出"窗口右上角三个位置也需要重新对比一下，应该是有点不对的"。

### 量法上的一个坑
第一次在 `appheader` 里量，只找到应用图标（IMG 20×16）和标题（SPAN 355×21），
**没有按钮**；在 `contextmenu` 里量到 4 个 `img.windowcontrols`（restore/minimize/
maximize/close `_classic.png`），但都是 **0×0** —— 那是**标题栏右键菜单的图标**，不是按钮。

**真正的按钮在窗口内、`appheader` 之外**：是 `<div>`，用 `background-image` 画。
在 `app` 元素内遍历"带 background-image 的元素"才找到。

### 实测（主题已切到 `luna blue`，窗口 466×301）
| 元素 | 尺寸 | 相对窗口的 x |
|---|---|---|
| `minimize.png` | **19 × 19** | 392 |
| `maximize.png` | **19 × 19** | 416 |
| `close.png` | **19 × 19** | 440 |
| `restore.png` | 0×0（未最大化时隐藏，最大化时替代 maximize） | — |

推出：**按钮 19×19，节距 24px（即间隙 5px）**，
close 右边缘距窗口右边 **7px**（440+19=459，窗口宽 466）。

### 我们原来的实现（不对）
`HeaderButtons.js`：按钮 **21×21**、`gap: 2px`（节距 23）→ 整体偏大、间隙偏小。

### 已改
`HeaderButtons.js`：`width/height: 21px → 19px`、`gap: 2px → 5px`。

**待确认**：close 距右边 7px 这一项还没核（我们那里是容器 `margin-right: 2px`
+ 窗口内边距，需要再量一次确认总偏移）。










